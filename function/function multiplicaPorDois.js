function multiplicarpordois (numero) {
    let resultado= numero * 2;
    return resultado
  }
  /*Esta é a definição da função multiplicarpordois.
Ela recebe um parâmetro chamado numero.
Dentro da função, o valor do parâmetro numero é multiplicado por 2 e armazenado na variável resultado.
Em seguida, a função retorna o valor armazenado em resultado.*/
alert (multiplicarpordois(6))
alert (multiplicarpordois(4))
alert (multiplicarpordois(10))

/* Esses comandos alert chamam a função multiplicarpordois com diferentes valores como argumentos (6, 4 e 10, respectivamente).
A função é chamada com cada um desses números como entrada, multiplicando cada um por 2.
Depois que a função é executada com cada valor, o resultado é exibido em um alert individual.*/